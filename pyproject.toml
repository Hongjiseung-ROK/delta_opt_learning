[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "delta_chem"
version = "0.1.0"
requires-python = ">=3.11"
description = "MMFF→DFT geometry correction pipeline for Gaussian 09"

[tool.setuptools.packages.find]
where = ["src"]
